# Friccion local - Stage 0

La bitacora global vive en `docs/friction/FRICTION_LOG.md`.

Entradas Stage 0:

- `F-0001`: WSL no visible desde sandbox, resuelto usando ejecucion fuera del sandbox.
- `F-0002`: instalacion `uv` atascada y NumPy corrupto, resuelto reinstalando con `UV_LINK_MODE=copy`.
