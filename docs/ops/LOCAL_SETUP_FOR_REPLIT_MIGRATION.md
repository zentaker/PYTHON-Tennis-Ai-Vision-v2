# Local Setup For Replit Migration

Date/time: 2026-05-22 23:48:03 -05:00

## Repo Location

Final repo path:

```text
C:\Users\Are\Desktop\tennisAI
```

The repository was initially nested at:

```text
C:\Users\Are\Desktop\tennisAI\TennisAI
```

The root folder did not contain `.git`, while `.\TennisAI\.git` existed. All children of `.\TennisAI` were moved into the root with hidden files included, then the now-empty `.\TennisAI` directory was removed.

## Git Baseline

HEAD before the ops commit:

```text
436a149b0d5af5447cafdf843ac0a4d405565035
```

Branch:

```text
main
```

Remote origin:

```text
https://github.com/zentaker/PYTHON-Tennis-Ai-Vision-v2.git
```

Initial status after moving the repo:

```text
## main...origin/main
```

## File Inventory

Confirmed present:

```text
README.md
pyproject.toml
uv.lock
.gitignore
src/
docs/
tests/
```

## Ignore Rules

`.gitignore` confirms these heavy or local artifacts are intentionally excluded:

- `outputs/*`, except `outputs/README.md`
- `models/*`, except `models/README.md`
- `third_party/`
- `.venv/`
- videos such as `*.mp4`, `*.mov`, `*.avi`, `*.mkv`, `*.webm`
- model weights such as `*.pth`, `*.pt`, `*.ckpt`, `*.onnx`, `*.safetensors`
- `data/reference_clip/*`, except the documented reference JSON/readme files

This means outputs, models, videos, checkpoints, and third-party source drops are not expected in Git by design.

## Python And Dependency Setup

Detected `python`:

```text
Python 3.11.6
C:\Program Files\Inkscape\bin\python.exe
```

Detected issues:

- `uv` is not installed.
- `py` is not available on PATH inside the normal shell.
- The external Python launcher exists, but `py -0p` only reports Python 3.12.
- The available `python` comes from Inkscape. It reports Python 3.11.6 but cannot create a working project venv.

Attempted setup:

```text
python -m venv .venv
```

Result:

```text
Error: Command '['C:\\Users\\Are\\Desktop\\tennisAI\\.venv\\bin\\python.exe', '-m', 'ensurepip', '--upgrade', '--default-pip']' returned non-zero exit status 103.
```

The partial `.venv` was inspected, confirmed to be generated by this session, and removed. No global package manager, global `uv`, model checkpoint, WASB dependency, or `third_party` payload was installed.

Install method used:

```text
Not completed. A standard Python 3.11 installation or uv is required.
```

Dependencies still missing for local lightweight tests:

- `pytest`
- `opencv-python` / `cv2`
- `scipy`

Tracker/WASB dependencies such as `torch` were not installed.

## Tests And Checks

Command:

```text
python scripts/verify_env.py
```

Result: failed with 4 critical issues.

Observed output summary:

- OK: Python 3.11.6
- FAIL: script requires WSL2/Linux for the final gate
- OK: NumPy 1.26.1
- FAIL: OpenCV / `cv2` is not installed
- FAIL: SciPy is not installed
- OK: `data`, `data/reference_clip`, `models`, and `outputs` exist
- FAIL: `models/wasb` is missing
- WARN: clip/frame validation skipped because OpenCV is unavailable

Command:

```text
pytest tests/test_event_loader.py
```

Result: failed because `pytest` is not installed.

Command:

```text
python -m pytest tests/test_event_loader.py
```

Result: failed because the current Python environment has no `pytest` module.

Additional file checks:

```text
tests/test_event_loader.py
tests/test_manual_annotation_validation.py
```

Result: both files are absent in the current repo snapshot.

Command:

```text
python scripts/replit_smoke_test.py
```

Result at report time: failed only at the required `event_loader` import.

Observed output summary:

- OK: `src`
- OK: `src.court.coordinates`
- OK: `src.tracker.trajectory_io`
- FAIL: no `src.events.event_loader`, `src.event_loader`, or top-level `event_loader` module exists

This does not run WASB, OpenCV, videos, checkpoints, or heavy processing.

## Final Git State

Expected after committing these ops notes:

```text
## main...origin/main [ahead 1]
```

No push was performed.

`.venv/` is ignored by `.gitignore`:

```text
.gitignore:21:.venv/
```

## Conclusion

Status: not fully ready for Replit import as a green smoke-test environment.

The repository structure is corrected and ready to be imported from GitHub after the ops commit is pushed. The current blockers are environment setup and missing lightweight event-loader assets:

- install a standard Python 3.11 runtime or `uv`;
- run `python -m pip install -e ".[dev]"` or `uv sync`;
- add or restore the intended lightweight `event_loader` module/test in the correct project stage;
- then rerun `python scripts/replit_smoke_test.py`.

Replit can still be used as an auxiliary code/docs workflow once the GitHub branch includes these ops notes, but it should not be used for WASB, videos, checkpoints, outputs, or `third_party` payloads at this stage.
