# Stage 5B v3 input inventory

Status: `CANONICAL_INPUTS_VERIFIED`.

The machine-readable inventory is `config/stage5b_v3/input_manifest.json`. It records
path, type, SHA-256, provenance/acceptance, unit, coordinate system, timestamp mode,
dependency, and intended use for every input. Critical sources are the refined Stage
5A.1 camera, accepted Stage 1 homography, real reduced Stage 3 VFR ball track, accepted
Stage 4 events, and accepted P1 serialization/artifact.

Historical v1/v2 code and rejection reports remain unchanged and are evidence only.
No missing critical input was substituted. The reduced real fixture covers frames
139–463 and preserves gaps rather than fabricating observations.
