# Stage 5A.2 extended ground-plane visual audit

`human_visual_approval: rejected`

Human-gate status: `STAGE5A2_REJECTED_BY_HUMAN_GATE_EVIDENCE_INSUFFICIENT`.

The six images show the detected line evidence, old/refined projections, independent
camera projection, foot estimates, stored/refined/camera ground context, the complete
doubles court plus 5 m baseline margins, and extrapolation uncertainty. Nothing outside
the court was claimed not to be clipped, but fixed axes clip ev_003 and ev_007. The
ten named lines are model lines evaluated against a general mask, not ten independently
detected/classified painted lines. Refinement did not improve the metrics, geometry
sources were correlated, uncertainty was incomplete, temporal support was absent, and
the foot sheet did not use real event frames. The 5 m backcourt rule is withdrawn.

Legend: red/green are old/refined court lines; magenta is the camera ground projection;
yellow circles are estimated foot pixels; error bars are metric uncertainty.
