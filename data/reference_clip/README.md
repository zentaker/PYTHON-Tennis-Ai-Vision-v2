# reference_clip/

Archivos locales del clip de referencia Nivel A.

Requeridos para cerrar Stage 0:

- `madrid_R1.mov`
- `reference_frame.png`
- `manual_annotation.json` para Stage 4

Usar `manual_annotation.example.json` como plantilla estructural o la herramienta
`tools/manual_event_annotator/index.html`. El ejemplo no contiene eventos reales. La
anotacion humana solo puede considerarse evidencia despues de revision y gate humano.
